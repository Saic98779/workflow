# Removed hardcoded secrets and ensured all secrets are read from Vault

# Local development database configuration
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://10.3.38.49:3306/prod_workflow

# Dev-only behavior override
spring.jpa.hibernate.ddl-auto=validate

# Dev-specific storage override
storage.file.directory.location=/opt/workflow/uploads-dev

# Security / app secrets
jwt.expiration=86400000

swagger.enabled=true


# Allowed file extensions
file.allowed.extensions=pdf,png,jpg,jpeg

# Max file size (optional, if you want custom logic)
file.max.size=5242880   # 5MB in bytes

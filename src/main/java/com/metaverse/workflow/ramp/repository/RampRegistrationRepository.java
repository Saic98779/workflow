package com.metaverse.workflow.ramp.repository;

import com.metaverse.workflow.model.RampRegistration;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RampRegistrationRepository extends JpaRepository<RampRegistration, Integer> {
}

